# Windows AI 直播助手（抖音）— 详细技术方案

> 本文档是可编码级的详细方案：读完即可照着目录结构和接口签名逐文件写代码。
> 快速恢复上下文可看 `项目上下文.md`（压缩摘要）。

---

## 1. 项目概述

一个运行在 Windows 上的 **AI 直播助手**（面向抖音直播间），用一句话说：

> 抓取直播间弹幕 → 自动判断该不该回、怎么回 → 给主播一条「建议回复」，同时提供提词器、话题生成、知识库管理。

### 1.1 核心功能

| 功能 | 说明 |
| --- | --- |
| 弹幕获取 | 从直播间拿到实时弹幕（默认**模拟数据**，可插拔真实 Provider） |
| AI 回复建议 | 对弹幕给出「建议回复文案」，主播点一下就能用 |
| 提词器 | 大字滚动显示台本/口播词 |
| 话题生成 | 基于弹幕/知识库生成聊天话题 |
| 知识库管理 | 维护主播的专属资料（产品、FAQ、人设），供 AI 检索 |

### 1.2 设计约束（贯穿全项目）

1. **省 token**：单场直播 < 50 次 AI 调用、< 2 元成本（见第 6 章三级阶梯）。
2. **低成本**：免费/开源优先，**核心功能零第三方依赖**（只用 Python 标准库）。
3. **学习友好**：Demo 级可运行、全程中文注释、结构清晰便于上手。
4. **可扩展**：模块化、接口抽象、可替换（Provider / AI 引擎 / 知识库后端）。

---

## 2. 技术选型与决策

| 维度 | 选型 | 理由 |
| --- | --- | --- |
| 界面 | **Tkinter**（标准库） | 零依赖、学习简单、随 Python 自带 |
| 弹幕来源 | **模拟数据 + 可插拔 Provider** | 无抖音环境也能跑通 Demo；真实抓取留作可选 |
| 本地 AI | **Ollama + Qwen3-4B** | 免费、离线、0 元/次，负责 L2 |
| 云端 AI | **DeepSeek deepseek-chat** | 便宜、吃前缀缓存、负责 L3 |
| 运行环境 | **Python 3.10**（本机命令 `py -3.10`） | 本机 3.10.8 可用，不用 3.14 |
| 知识库 | **SimpleKB（内置，零依赖）** + 可选 **Chroma** | 简单检索零依赖；语义检索可选增强 |
| HTTP 调用 | **标准库 `urllib`** | 满足「核心零第三方依赖」约束 |

> 决策原则：**能标准库就不用第三方，能免费就不用付费，能本地就不上云。**

---

## 3. 整体架构与数据流

### 3.1 分层

```
┌─────────────────────────────────────────────────┐
│  ui/        界面层（Tkinter 面板）                │
├─────────────────────────────────────────────────┤
│  core/      协调层（Coordinator + BudgetGuard）   │
├──────────────┬──────────────┬───────────────────┤
│  danmaku/    │  ai/         │  kb/  rules/      │
│  弹幕源      │  AI 引擎     │  知识库 / 规则     │
└──────────────┴──────────────┴───────────────────┘
```

- **ui** 只做展示与交互，不碰业务逻辑。
- **core** 是大脑：订阅弹幕、聚合、决定走哪一级回复、控制预算。
- **danmaku / ai / kb / rules** 都是「能力模块」，通过接口被 core 调用，可替换。

### 3.2 核心数据流

```
DanmakuProvider（mock / douyin）
        │ 回调 on_danmaku(DanmakuEvent)
        ▼
Coordinator ── 聚合窗口（5 条 或 20 秒）──► 一批弹幕
        │
        ├─ L1 模板命中？  ──是──► 直接回复（0 token / 0 元）
        ├─ L2 本地分类/简单回复？（Ollama，0 元）
        └─ L3 云端批量生成？（DeepSeek，预算内，最少调用）
        │
        ▼
ReplySuggestion ──► EventBus ──► ui/reply_panel 显示建议
```

---

## 4. 目录结构

```
直播软件/
├── main.py                 # 入口：加载配置 → 组装各模块 → 启动 UI
├── config.json             # 配置文件（danmaku/aggregate/ai/budget/kb）
├── requirements.txt        # 依赖声明（核心零依赖，chromadb 可选）
├── README.md               # 使用说明
├── 方案.md                 # 本文档
├── 项目上下文.md           # 压缩摘要（快速恢复上下文）
├── core/                   # 协调层
│   ├── events.py           # 数据模型（DanmakuEvent 等）+ 事件总线
│   ├── budget.py           # BudgetGuard 预算器
│   └── coordinator.py      # 协调器：聚合 + 三级阶梯调度
├── danmaku/                # 弹幕源
│   ├── base.py             # DanmakuProvider 抽象基类
│   ├── mock.py             # 模拟弹幕（定时生成，演示用）
│   └── douyin.py           # 抖音真实接入（占位）
├── ai/                     # AI 引擎
│   ├── prompts.py          # 精简 prompt 模板（吃前缀缓存）
│   ├── local.py            # 本地 Ollama + Qwen3-4B
│   └── cloud.py            # 云端 DeepSeek deepseek-chat
├── kb/                     # 知识库
│   └── store.py            # SimpleKB（内置）+ ChromaKB（可选）
├── rules/                  # 规则
│   ├── templates.py        # L1 模板（关键词 → 回复，0 token）
│   └── sensitive.py        # 敏感词过滤
└── ui/                     # 界面
    ├── app.py              # 主窗口，组装各面板
    ├── danmaku_panel.py    # 弹幕列表面板
    ├── reply_panel.py      # 回复建议面板
    ├── teleprompter.py     # 提词器面板
    └── kb_panel.py         # 知识库管理面板
```

---

## 5. 模块详细设计（接口签名）

> 约定：全部兼容 Python 3.10；类型注解用 `from __future__ import annotations` 保持简洁；模块间只通过构造参数注入依赖，不互相 import 具体实现（依赖倒置）。

### 5.1 `core/events.py` — 数据模型与事件总线

```python
from dataclasses import dataclass, field

@dataclass
class DanmakuEvent:
    """一条弹幕"""
    user: str                      # 用户名
    content: str                   # 弹幕内容
    timestamp: float               # 时间戳（time.time()）

@dataclass
class ReplySuggestion:
    """一条回复建议"""
    content: str                   # 建议回复的文案
    source: str                    # 来源："template" / "local" / "cloud" / "fallback"
    cost: float = 0.0              # 本次估算费用（元）
    matched: list = field(default_factory=list)  # 命中的弹幕内容列表

class EventBus:
    """极简事件总线：按事件类型维护回调列表，零依赖。

    只定义三种事件类型常量：EVT_DANMAKU / EVT_REPLY / EVT_LOG。
    """
    def subscribe(self, event_type: str, handler) -> None: ...
    def publish(self, event_type: str, data) -> None: ...
```

### 5.2 `core/budget.py` — 预算器

```python
class BudgetGuard:
    """控制 AI 调用次数与费用，超限自动降级。

    max_calls=50（次）、max_cost=2.0（元）。
    """
    def __init__(self, max_calls: int = 50, max_cost: float = 2.0): ...

    def can_call_cloud(self) -> bool:
        """是否还允许调用云端（次数与费用都没超限）"""

    def record_call(self, cost: float = 0.0) -> None:
        """记录一次云端调用及其估算费用"""

    @property
    def remaining_calls(self) -> int: ...
    @property
    def spent(self) -> float: ...
```

### 5.3 `core/coordinator.py` — 协调器

```python
class Coordinator:
    """核心大脑：聚合弹幕，按 三级阶梯 依次尝试回复。"""
    def __init__(self, config, event_bus, budget,
                 templates, sensitive, local_ai, cloud_ai, kb): ...

    def on_danmaku(self, event: DanmakuEvent) -> None:
        """弹幕入口：进聚合窗口，不立即回复"""

    def _flush(self) -> None:
        """聚合窗口满（5 条 或 20 秒）时触发，处理一批弹幕"""

    def _try_l1_template(self, batch) -> ReplySuggestion | None:
        """L1：模板命中，0 token / 0 元"""

    def _try_l2_local(self, batch) -> ReplySuggestion | None:
        """L2：本地 Ollama 分类/简单回复，0 元"""

    def _try_l3_cloud(self, batch) -> ReplySuggestion | None:
        """L3：云端 DeepSeek 批量生成（先过 BudgetGuard）"""

    def start(self) -> None: ...   # 启动聚合定时器
    def stop(self) -> None: ...    # 停止定时器
```

> 三级阶梯的**优先级**：L1 命中就直接返回，不往下走；L1 未命中再试 L2；L2 不可用/未命中再试 L3；L3 被预算拦截则降级为「不回」或模板兜底（`source="fallback"`）。

### 5.4 `danmaku/base.py` — Provider 抽象基类

```python
from abc import ABC, abstractmethod

class DanmakuProvider(ABC):
    """弹幕源抽象基类：所有弹幕源都实现 start/stop，通过 on_danmaku 回调吐弹幕。"""
    def __init__(self, on_danmaku):      # on_danmaku: Callable[[DanmakuEvent], None]
        self.on_danmaku = on_danmaku

    @abstractmethod
    def start(self) -> None: ...
    @abstractmethod
    def stop(self) -> None: ...
```

### 5.5 `danmaku/mock.py` — 模拟弹幕

```python
class MockProvider(DanmakuProvider):
    """演示用：后台线程按固定间隔生成模拟弹幕。"""
    def __init__(self, on_danmaku, samples: list[str] | None = None,
                 interval: float = 2.0): ...
    def start(self) -> None:   # 起 daemon 线程，循环 random.choice(samples)
    def stop(self) -> None:    # 置停止标志
```

内置若干条典型弹幕样例（问产品、求回复、闲聊等），便于演示 L1/L2/L3 各层级。

### 5.6 `danmaku/douyin.py` — 抖音真实接入（占位）

```python
class DouyinProvider(DanmakuProvider):
    """抖音直播间弹幕抓取——占位实现。

    接口已定义，默认不启用。真实接入需处理抖音 WebSocket 协议
    （签名、房间号、心跳等），成本与不确定性高，作为可选后续。
    接入点：实现 start() 中建立连接、解析弹幕并回调 on_danmaku。
    """
    def start(self) -> None:
        raise NotImplementedError("抖音弹幕接入待实现，默认请使用 MockProvider")
    def stop(self) -> None: ...
```

### 5.7 `ai/prompts.py` — prompt 模板

```python
# 固定前缀：保持稳定可命中 DeepSeek 前缀缓存，降低 token 成本
SYSTEM_PROMPT = "你是抖音直播间的主播助手，根据弹幕给出简短、口语化、可复用的建议回复。"

def build_batch_prompt(danmaku_list: list[str]) -> str:
    """把一批弹幕拼成一个精简的批量请求文本，一次调用生成多条建议。"""
```

> prompt 设计要点：**前缀固定、指令精简、批量处理**，三管齐下省 token。

### 5.8 `ai/local.py` — 本地 Ollama

```python
class LocalAI:
    """本地 Ollama 引擎（Qwen3-4B），0 元/次，用 urllib 调 /api/chat。"""
    def __init__(self, base_url: str = "http://localhost:11434",
                 model: str = "qwen3:4b", timeout: float = 60.0): ...
    def chat(self, messages: list[dict]) -> str:
        """POST {base_url}/api/chat，返回 content 文本；失败抛异常由上层降级。"""
    def is_available(self) -> bool:
        """探测 Ollama 是否在线（GET /api/tags）。"""
```

### 5.9 `ai/cloud.py` — 云端 DeepSeek

```python
class CloudAI:
    """云端 DeepSeek（deepseek-chat），OpenAI 兼容接口，用 urllib 调用。"""
    def __init__(self, api_key: str, model: str = "deepseek-chat",
                 base_url: str = "https://api.deepseek.com", timeout: float = 60.0): ...
    def chat(self, messages: list[dict]) -> str:
        """POST {base_url}/chat/completions，返回 choices[0].message.content。"""
```

### 5.10 `kb/store.py` — 知识库

```python
from abc import ABC, abstractmethod

class KBStore(ABC):
    """知识库抽象：新增 / 检索 / 删除 / 全量列表。"""
    @abstractmethod
    def add(self, text: str, meta: dict | None = None) -> str: ...   # 返回条目 id
    @abstractmethod
    def search(self, query: str, top_k: int = 3) -> list[dict]: ... # 返回 [{id, text, score}]
    @abstractmethod
    def delete(self, id: str) -> None: ...
    @abstractmethod
    def list_all(self) -> list[dict]: ...

class SimpleKB(KBStore):
    """内置零依赖实现：关键词/子串匹配 + 简单打分排序，适合小数据量。"""

class ChromaKB(KBStore):
    """可选语义检索：依赖 chromadb + bge-m3 向量模型，需额外安装。"""
```

### 5.11 `rules/templates.py` — L1 模板

```python
# 模板结构：{"keywords": [...], "reply": "..."}，命中任一关键词即返回
TEMPLATES: list[dict] = [...]

def match_template(content: str) -> str | None:
    """命中返回模板回复文本，未命中返回 None。0 token。"""
```

内置示例：问价格、求关注、欢迎新人、感谢礼物等高频场景。

### 5.12 `rules/sensitive.py` — 敏感词

```python
SENSITIVE_WORDS: list[str] = [...]

def filter_text(text: str) -> str:
    """把敏感词替换为 *（长度对齐），返回过滤后的文本。"""
```

### 5.13 `ui/*.py` — 界面面板

```python
# danmaku_panel.py
class DanmakuPanel(ttk.Frame):
    """弹幕列表：滚动显示历史弹幕。"""
    def append(self, event: DanmakuEvent) -> None: ...

# reply_panel.py
class ReplyPanel(ttk.Frame):
    """回复建议：显示最新建议 + 来源标签 + 一键复制。"""
    def show(self, suggestion: ReplySuggestion) -> None: ...

# teleprompter.py
class TeleprompterPanel(ttk.Frame):
    """提词器：可编辑台本，滚动播放/字号调节。"""

# kb_panel.py
class KBPanel(ttk.Frame):
    """知识库管理：增删条目、关键词检索、绑定 KBStore。"""

# app.py
class App(tk.Tk):
    """主窗口：加载配置、装配模块、布局各面板、启动 Coordinator 与 Provider。"""
```

---

## 6. 省 token 三级阶梯（核心机制）

```
弹幕 ──► L1 模板命中（0 token / 0 元）
        └─ 未命中 ──► L2 本地分类/简单回复（Ollama，0 元）
                      └─ 不可用/未命中 ──► L3 云端批量生成（DeepSeek，最少调用）
                                           └─ 超预算 ──► 降级为不回/模板兜底
```

| 层级 | 引擎 | 成本 | 触发条件 | 说明 |
| --- | --- | --- | --- | --- |
| L1 | 本地模板 | 0 token / 0 元 | 弹幕命中 `templates.py` 关键词 | 高频问题直接秒回 |
| L2 | Ollama + Qwen3-4B | 0 元 | L1 未命中 且 Ollama 在线 | 本地跑，不花钱 |
| L3 | DeepSeek | 按量计费 | L1/L2 都未解决 | 批量生成，预算内 |

配套省 token 手段：

1. **聚合窗口**：攒够 5 条弹幕或满 20 秒才处理一批，把多次调用合并成一次。
2. **BudgetGuard 预算器**：上限 50 次 / 2 元，超限自动降级到 L1/L2，绝不透支。
3. **精简 prompt**：固定前缀 + 批量请求，一次调用产多条建议。
4. **吃 DeepSeek 前缀缓存**：`SYSTEM_PROMPT` 与指令前缀保持稳定，命中缓存省 token。

---

## 7. 数据模型汇总

- `DanmakuEvent(user, content, timestamp)` —— 一条弹幕。
- `ReplySuggestion(content, source, cost, matched)` —— 一条回复建议，`source ∈ {template, local, cloud, fallback}`。
- 知识库条目统一为 `{"id": str, "text": str, "score": float | None, ...meta}`。

---

## 8. 配置设计（`config.json`）

```json
{
  "danmaku":    { "provider": "mock", "mock_interval": 2.0 },
  "aggregate":  { "count": 5, "seconds": 20 },
  "ai": {
    "local":  { "enabled": true,  "base_url": "http://localhost:11434", "model": "qwen3:4b" },
    "cloud":  { "enabled": false, "api_key": "", "base_url": "https://api.deepseek.com", "model": "deepseek-chat" }
  },
  "budget":     { "max_calls": 50, "max_cost": 2.0 },
  "kb":         { "backend": "simple" }
}
```

- `danmaku.provider`：`mock`（默认）或 `douyin`（占位）。
- `ai.cloud.enabled` 默认 `false`：无 key 也能跑通；填入 `api_key` 并置 `true` 启用云端。
- `kb.backend`：`simple`（默认）或 `chroma`（需额外安装）。

---

## 9. 依赖说明（`requirements.txt`）

核心功能 **零第三方依赖**，仅 Python 3.10 标准库：
`tkinter`、`urllib`、`json`、`threading`、`queue`、`dataclasses`、`random`、`time`。

可选依赖（用不到可不装）：

```
# 语义检索（可选）
# chromadb>=0.4.0
```

---

## 10. 实现顺序（里程碑）

按依赖从底层到顶层，每步都保持「可运行、可验证」：

1. `config.json` + `requirements.txt` + `main.py` 骨架 —— 项目能启动。
2. `core/events.py`（数据模型 + 事件总线）→ `rules/`（L1 模板、敏感词，独立可测）。
3. `danmaku/`（base + mock）—— 模拟弹幕数据流跑通。
4. `kb/store.py`（SimpleKB）—— 本地知识库增删检索可用。
5. `ai/`（prompts + local + cloud）—— L2/L3 引擎就绪。
6. `core/budget.py` + `core/coordinator.py` —— 三级阶梯串联，业务闭环。
7. `ui/`（五面板 + app）—— 界面落地，端到端可用。
8. 验证（见第 11 章）。

---

## 11. 验证方式

```bash
# 1) 语法检查（全部 .py）
py -3.10 -m py_compile main.py core/*.py danmaku/*.py ai/*.py kb/*.py rules/*.py ui/*.py

# 2) 无 key / 无 Ollama 冒烟测试：模拟弹幕 → 模板回复跑通
py -3.10 main.py

# 3) 有 Ollama 时验证 L2 本地回复
# 4) 有 DeepSeek key 时（config.json 开启 cloud）验证 L3 云端批量
```

**验收标准**：
- 不装任何第三方包、不填 key、不启动 Ollama，`main.py` 也能正常启动，模拟弹幕持续刷出，且命中的高频弹幕能给出模板回复（L1）。
- 启动 Ollama 后，未命中模板的弹幕走 L2 本地回复。
- 填入 DeepSeek key 后，L1/L2 未解决的批次走 L3 云端，且调用次数/费用受 BudgetGuard 限制。

---

## 12. 关键约定与注意事项

1. **依赖倒置**：`core` 只依赖抽象接口（`DanmakuProvider`、`KBStore`、AI 的 `chat`），不 import 具体实现；具体实现由 `main.py` 装配注入。
2. **线程安全**：弹幕来自后台线程，UI 更新必须通过 Tkinter 的 `after()` / 线程安全队列回到主线程，避免跨线程改 UI。
3. **优雅降级**：任何 AI 调用失败或不可用，都要能回退到下一级或模板兜底，绝不因异常卡死直播。
4. **中文注释**：所有代码中文注释，面向学习。
