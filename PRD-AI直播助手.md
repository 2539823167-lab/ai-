# PRD：AI 直播助手（抖音弹幕实时回复助手）

> **文档版本**：V1.0 ｜ **产品形态**：Windows 桌面工具（Demo 可运行）
> **编写日期**：2026-09-02 ｜ **作者**：张怡康
> **状态**：评审稿 ｜ **配套材料**：可运行 Demo（见 `readme.md` / `方案.md` / `tests/`）

---

## 1. 文档说明

### 1.1 背景与机会

抖音直播已成为中小商家与个人主播最重要的经营场域之一。主播在直播中需要
**同时**完成讲解、控场、带货、答疑，而观众弹幕是实时的、并发的、口语化的，
普遍存在以下真实痛点：

| 痛点 | 现象 | 代价 |
| --- | --- | --- |
| **看不过来** | 弹幕刷屏，高频问题（价格/发货/怎么买）反复被问 | 老粉问题无人应答，转化流失 |
| **回不过来** | 一次只能回一条，观众提问排队被淹没 | 互动率低，直播间热度起不来 |
| **不会回** | 收到质疑/带节奏/复杂提问，一时不知道怎么接 | 冷场或回复不当引发舆情 |
| **精力分散** | 要记产品参数、活动口径、话术，一边讲一边翻资料 | 主播疲惫，话术不统一 |
| **AI 成本顾虑** | 用大模型 API 直连弹幕，按次按 token 计费，场次一长费用失控 | 不敢规模化使用 AI |

**机会点**：将「大模型能力」与「直播运营规则」结合，做一个**主播侧低成本的
AI 副驾**——不是替主播说话，而是帮主播**更快、更准、更省**地决定「回什么、怎么回」，
并沉淀主播自己的知识库与人设口径。

### 1.2 一句话定位

> 一款 Windows 端的 AI 直播副驾：实时抓取直播间弹幕 → 自动判断该不该回、怎么回 →
> 给主播一条「可直接采纳的建议回复」，并附带提词器、话题生成与知识库管理，
> 用 **「模板优先 → 本地免费 → 云端兜底」** 的三级阶梯把单场 AI 成本压到最低。

### 1.3 产品目标（本版本）

1. **提效**：高频问题（价格/关注/道谢/下单等）0 秒模板秒回，主播不再被重复提问消耗。
2. **降本**：单场直播云端调用 ≤ 50 次、费用 ≤ 2 元（预算硬约束，超限自动降级）。
3. **可用**：无 API Key、无本地模型也能完整跑通全流程（Demo 级优雅降级）。
4. **沉淀**：主播的知识库随用随建、重启不丢，AI 回复口径与主播人设一致。

---

## 2. 目标用户与使用场景

### 2.1 用户画像

| 画像 | 描述 | 核心诉求 |
| --- | --- | --- |
| **个人主播 / 中小商家**（主目标） | 抖音带货或才艺直播，单人出镜，无专职运营 | 一个人也能接住弹幕互动；口径统一；不想为 AI 花太多钱 |
| **兼职/新手主播** | 刚起步，话术生疏、容易冷场 | 有人「递话」：提示怎么接、给话题、给口播稿 |
| **直播运营 / 场控**（次目标） | 帮主播盯屏、递话、维护氛围 | 多屏信息聚合、可回看、可沉淀话术素材 |
| **技术学习 / Demo 评审者** | 想低成本体验「AI + 直播」产品逻辑 | 零第三方依赖即可运行，逻辑透明可解释 |

### 2.2 典型使用场景

- **场景 A：高频问答不丢单。** 主播正讲产品，弹幕连续刷「多少钱」「怎么下单」「有没有小样」。
  系统在弹幕流旁给出模板建议，主播扫一眼点「✅ 采纳上屏」，三秒完成应答闭环。
- **场景 B：复杂提问不冷场。** 弹幕「这款和上一款有什么区别？」，模板未命中，
  本地 Ollama（0 元）或云端 DeepSeek 生成口语化对比话术供主播参考。
- **场景 C：没有 AI 也能演示。** 新人主播无 Key、未装模型，用「模拟弹幕」模式即可
  看到完整界面与模板秒回效果，用于演示、教学或面试作品展示。
- **场景 D：直播冷场救场。** 观众活跃度下降时，点击「生成话题」，系统基于近期弹幕 +
  主播知识库抛出一个能带动互动的话题，主播照着念即可。
- **场景 E：知识沉淀。** 主播把「发货政策」「售后口径」写进知识库或导入文档，
  之后 AI 生成话题、回复建议都会带上这些口径，避免前后说法不一致。

---

## 3. 竞品与替代方案分析

| 替代方案 | 优势 | 劣势 | 本项目切入点 |
| --- | --- | --- | --- |
| **纯人工盯屏** | 零成本、最懂直播 | 看不过来、口径不统一、易疲劳 | 聚合窗口 + 建议回复，帮人更快决策 |
| **把弹幕整段丢给大模型 API** | 灵活通用 | 每次调用都花钱、prompt 不稳定、无成本防线 | 三级阶梯 + 预算器 + 前缀缓存，成本可控 |
| **直播平台自带「快捷回复」** | 在平台内、即点即用 | 纯固定文案、不能结合实时弹幕与知识库 | 动态建议 + 可采纳上屏，保留人工决策权 |
| **同品类 AI 场控工具（商业化）** | 功能全、有 SaaS 后台 | 订阅贵、绑定平台、隐私顾虑 | 本地优先（Ollama）+ 数据本地化 + 免费核心 |

**差异化结论**：本项目不追求「全自动替主播发言」，而是做**「低成本的直播副驾」**：
人（主播）做最终决策，AI 做识别、聚合、起草、省钱。这一产品主张也天然规避了
「AI 说错话」带来的直接舆情风险。

---

## 4. 成功指标（怎么算做成）

### 4.1 北极星指标

> **单场直播建议采纳率 = 主播点击「采纳上屏」的建议数 ÷ 系统给出的建议数**

采纳 = 主播真正用了，是「有效帮助」的最直接证据。

### 4.2 核心指标（对应功能目标）

| 指标 | 定义 | 目标（Demo 参照） | 数据来源 |
| --- | --- | --- | --- |
| 建议采纳率 | 采纳数 / 建议数 | ≥ 30% | 客户端埋点 |
| 模板命中率 | 模板回复数 / 总建议数 | ≥ 50%（高频问题为主时） | 驾驶舱统计卡 |
| 弹幕应答时效 | 弹幕入聚合窗口到给出建议的时长 | 模板 < 1s；AI 类 ≤ 聚合上限+请求时长 | 运行日志/埋点 |
| 单场云端花费 | BudgetGuard 统计 | ≤ 2 元（硬上限 50 次） | 驾驶舱统计卡/状态栏 |
| 每百条弹幕云端调用 | 云端调用 / 弹幕数 × 100 | ≤ 8 次 | 日志聚合 |
| 知识库规模与复用 | 条目数、话题生成引用数 | 主播自建 ≥ 20 条 | 知识库面板 |

---

## 5. 需求范围与优先级

| 编号 | 需求 | 优先级 | 版本 |
| --- | --- | --- | --- |
| FR-01 | 弹幕接入与启停（模拟/抖音真实源） | P0 | V1.0（已实现） |
| FR-02 | 弹幕聚合窗口（攒 N 条 / M 秒） | P0 | V1.0（已实现） |
| FR-03 | 三级阶梯 AI 回复建议（模板/本地/云端） | P0 | V1.0（已实现） |
| FR-04 | 建议采纳上屏 / 一键复制（回复闭环） | P0 | V1.0（已实现） |
| FR-05 | 手动发送弹幕 + ⚡立即处理（演练入口） | P0 | V1.0（已实现） |
| FR-06 | 云端预算控制 BudgetGuard | P0 | V1.0（已实现） |
| FR-07 | 敏感词过滤与兜底降级 | P0 | V1.0（已实现） |
| FR-08 | 知识库管理（增删/检索/导入/持久化） | P1 | V1.0（已实现） |
| FR-09 | 话题生成（弹幕+知识库驱动） | P1 | V1.0（已实现） |
| FR-10 | 提词器（口播词/字号/自动滚动） | P1 | V1.0（已实现） |
| FR-11 | 驾驶舱：统计卡 + 状态灯 + 运行日志 | P1 | V1.0（已实现） |
| FR-12 | 历史建议回看（按来源着色） | P2 | V1.0（已实现） |
| FR-13 | 配置外置（config.json / 环境变量） | P2 | V1.0（已实现） |
| FR-14 | 语义检索知识库（Chroma，可选后端） | P2 | V1.0（可选启用） |
| FR-15 | 抖音断线自动重连与协议自愈 | P2 | V1.0（已实现） |
| FR-16 | 自动化回归测试（纯标准库） | P2 | V1.1（已实现） |
| FR-17 | 建议多选/采纳即回复到平台（连发） | P3 | V2.0 规划 |
| FR-18 | 多直播间/多主播账号切换 | P3 | V2.0 规划 |
| FR-19 | 话术资产库（复盘导出/团队共享） | P3 | V2.0 规划 |

> P0：闭环必需；P1：体验与竞争力；P2：增强与工程质量；P3：规划中不承诺。

---

## 6. 功能需求详述

> 每项需求给出：**需求描述 → 使用路径 → 交互与业务规则 → 验收标准（可测试）**。
> 标注（已实现）的需求，验收标准以当前 Demo + `tests/` 回归用例为基线。

### FR-01 弹幕接入与启停

- **描述**：系统从弹幕源持续接收弹幕。默认「模拟弹幕」保证无网也能演示；真实模式接入抖音直播间。
- **使用路径**：配置 `danmaku.provider` → 启动即自动拉取 → 头部「⏸ 暂停/▶ 继续」随时控制。
- **规则**：模拟源默认 2 秒一条（可配）；真实源断线自动重连；暂停仅暂停模拟生成，**手动发送不受影响**。
- **验收**：mock 模式启动 5 秒内弹幕流出现消息；点暂停后自动弹幕停止、点继续恢复；抖音模式房间解析失败给出明确错误日志且不崩溃。

### FR-02 弹幕聚合窗口

- **描述**：把多条弹幕合并成「一批」再处理，减少 AI 调用次数（省 token 的核心手段之一）。
- **使用路径**：回复建议面板「攒 N 条 / M 秒」在线调节。
- **规则**：两个阈值任一先到即触发处理；运行中可改、改完即生效；界面显示「待处理 N 条」。
- **验收**：默认 5 条/20 秒下，未满 5 条不回复；调节控件修改后按新阈值触发（回归用例覆盖条数阈值）。

### FR-03 三级阶梯 AI 回复建议

- **描述**：对每一批弹幕按 **L1 模板 → L2 本地 AI → L3 云端 AI** 依次尝试，命中即返回、不向下走。
- **使用路径**：弹幕批量到达 → 自动产出建议 → 展示在回复建议卡片（含来源徽章 + 针对弹幕 + 成本）。
- **规则**：
  - L1：本地模板关键词匹配，0 token/0 元（价格、关注、新人、道谢、下单、砍价、小样、开播时间等高频场景）；
  - L2：Ollama + Qwen3-4B，0 元；失败进入 60 秒可用性缓存，到期自动重试；
  - L3：DeepSeek `deepseek-chat`，批量请求一次产出 1~3 条，受 FR-06 预算限制；
  - 三级全无结果 → 输出「兜底」建议，绝不让界面空着。
- **验收**：模板句命中即 template；本地挂掉自动转云端；云端超预算自动降级（均有回归用例）。

### FR-04 建议采纳上屏（回复闭环）

- **描述**：主播认可建议后一键「采纳」：文案复制到剪贴板 + 以「📤 主播」消息显示在弹幕流，形成「观众提问 → AI 建议 → 主播回复」闭环。
- **验收**：采纳后剪贴板内容 = 建议文案；弹幕流出现绿色主播回复行。

### FR-05 手动发送弹幕 + ⚡立即处理

- **描述**：手动输入一条弹幕（模拟观众发言，青色高亮），走与真实弹幕完全相同的处理管线；或点击「⚡立即处理」不等窗口攒满、马上处理已有弹幕。
- **规则（重要）**：所有涉及 AI 的处理都放**后台线程**执行，UI 主线程不被阻塞（本版已修复：采纳/发送/立即处理不卡界面）。
- **验收**：处理期间界面可正常滚动、无假死；手动弹幕在弹幕流高亮显示。

### FR-06 云端预算控制 BudgetGuard

- **描述**：单场直播云端调用上限 **50 次 / 2 元**，达到任一上限后 `can_call_cloud()` 返回 False，L3 自动降级。
- **验收**：达到次数上限后云端调用计数不再增长，后续批次走模板/兜底（回归用例覆盖）。

### FR-07 敏感词过滤与兜底降级

- **描述**：进入模板/AI 前先清洗敏感词（等长 `*` 打码），避免脏词污染 AI 输出；任何 AI 失败都不抛错中断直播，逐级降级。
- **验收**：含敏感词的弹幕被清洗后再匹配/请求；AI 全部不可用时仍产出兜底建议不崩溃（回归用例覆盖）。

### FR-08 知识库管理

- **描述**：主播维护专属资料（产品/FAQ/人设/活动口径），供话题生成引用；支持检索与导入。
- **交互**：列表浏览、双击复制；关键词检索；底部新增条目；「添加文件」导入 `.txt/.md/.pdf`（500 字自动分块，标注来源）；删除选中。
- **规则（本版新增）**：SimpleKB 条目**自动持久化**到 `kb_data/simple_kb.json`（重启不丢、零依赖）；检索后端可在 `simple`（关键词）与 `chroma`（语义向量）间切换。
- **验收**：新增→重启程序→条目仍在（回归用例覆盖）；导入文件后列表出现带来源标注的条目；删除后检索不到。

### FR-09 话题生成

- **描述**：点击「生成话题」，系统取最近弹幕 + 知识库相关要点，生成「一个可带动互动的话题 + 一个抛给观众的问题」。
- **规则**：优先本地（0 元）→ 云端（预算内）；都在后台线程执行；无可生成素材或无可用的 AI 时给出明确提示文案。
- **验收**：本地可用时返回话题并写入提词器；无 AI 时提示「启动本地 Ollama 或配置云端 key」。

### FR-10 提词器

- **描述**：大字口播区：手动编辑台本、字号 ±、自动滚动（可调速、滚到底自动停）、一键清空、接收生成话题。
- **验收**：滚动开关与速度滑杆即时生效；滚到底自动停止。

### FR-11 驾驶舱：统计卡 + 状态灯 + 运行日志

- **描述**：顶部状态灯（弹幕源 / Ollama 每 15s 自动探测 / 云端配置）；统计卡（弹幕总数 / 回复建议 / 模板命中率 / 云端花费）；底部日志实时展示「三级阶梯走向」；状态栏显示待处理条数与剩余云端预算。
- **规则（本版优化）**：云端「未配置」显示为灰色（≠ 故障），不再误报红色告警。
- **验收**：Ollama 在线/离线状态灯 15 秒内翻转；统计数字随事件实时增长；日志保留最近 300 行。

### FR-12 历史建议回看

- **描述**：建议按来源（模板/本地/云端/兜底）着色，最新置顶，点击任意历史条目回看详情。
- **验收**：历史列表点击后主卡片切换为对应建议且不再重复入历史。

### FR-13 配置外置

- **描述**：全部配置集中在 `config.json`；API Key 优先读环境变量 `DEEPSEEK_API_KEY`（避免明文入库）。
- **验收**：改配置重启即生效；不填 Key、不装模型时程序照常运行。

### FR-14 语义检索知识库（可选）

- **描述**：`kb.backend = "chroma"` 时用 Chroma + Ollama bge-m3 向量检索，话题素材与检索质量更高。
- **验收**：安装 chromadb 并拉取 bge-m3 后切换配置，语义相近但无关键词重合的查询能召回相关条目。

### FR-15 抖音断线自动重连

- **描述**：真实弹幕模式下，WebSocket 断线后按固定间隔自动重连；房间解析失败给出明确提示。
- **验收**：模拟断线后自动恢复拉流（协议如被平台变更则以日志提示人工更新）。

### FR-16 自动化回归测试（工程质量）

- **描述**：`tests/` 内置纯标准库 `unittest` 用例，覆盖模板、敏感词、预算器、知识库检索与持久化、文档分块、协调器三级阶梯/聚合/降级等 27 个用例。
- **运行**：`py -3.10 -m unittest discover -s tests -t . -v`
- **验收**：全量用例通过；改动核心逻辑后可一键回归。

---

## 7. 核心机制说明（省成本三级阶梯）

```
一批弹幕（聚合窗口攒满 N 条 或 M 秒）
   │
   ├─ L1 本地模板命中？ ──────── 是 → 模板建议（0 token / 0 元，秒回）
   │     否 ↓
   ├─ L2 本地 Ollama 可用？ ──── 是 → 本地建议（0 元；失败进入 60s 缓存）
   │     否 ↓
   ├─ L3 云端预算内？ ────────── 是 → 云端建议（1 次 / 约 0.01 元）
   │     否 ↓
   └─ 兜底：输出「可忽略」提示，界面不空
```

| 决策点 | 业务规则 |
| --- | --- |
| 何时处理 | 攒满条数或到时间，取先到者；也可手动「立即处理」 |
| L1 命中即止 | 模板覆盖高频场景，**把 80% 重复提问挡在 0 成本层** |
| L2 失败缓存 | 失败后 60 秒内跳过 L2，避免对不可用服务反复打请求 |
| L3 预算闸门 | 次数 ≤ 50 且金额 ≤ 2 元才放行；超限自动回落 L1/兜底 |
| 并行安全 | 同一时刻只处理一批（flush 锁），后台线程不阻塞弹幕接收 |
| 回复闭环 | 主播始终是最终决策人：建议可采纳、可忽略、可回看 |

---

## 8. 信息架构与界面说明

### 8.1 布局（单窗口四象限）

```
┌────────────────────────────────────────────────────────────┐
│ 头部：🎙️ 标题  状态灯(弹幕源/Ollama/云端)  ⏸暂停弹幕       │
│ 统计卡：弹幕总数 │ 回复建议 │ 模板命中率 │ 云端花费          │
├──────────────────────┬─────────────────────────────────────┤
│ 💬 弹幕流            │ 💡 回复建议                          │
│ （时间/彩色昵称/内容）│   ⚡立即处理 攒N条/M秒              │
│   手动发送高亮       │   建议卡片(来源徽章+成本+针对弹幕)    │
│   主播采纳上屏       │   📋复制 ✅采纳上屏                  │
│                      │   历史建议(点击回看)                 │
├──────────────────────┼─────────────────────────────────────┤
│ 📚 知识库            │ 📜 提词器                            │
│  检索/条目列表       │  生成话题 字号± 清空 ▶滚动           │
│  新增/添加文件/删除  │  滚动速度滑杆 + 大字口播区           │
├──────────────────────┴─────────────────────────────────────┤
│ 📜 运行日志（三级阶梯走向，保留 300 行）                     │
│ 状态栏：待处理 N 条 · 云端预算 x/50 次                       │
└────────────────────────────────────────────────────────────┘
```

### 8.2 关键页面流

1. 启动即进入驾驶舱：弹幕自动刷出 → 建议卡片更新 → 状态灯/统计/日志联动。
2. 主播「采纳」建议 → 上屏一条绿色主播回复（闭环可见）。
3. 弹幕变少/冷场 → 提词器「生成话题」→ 结果写入提词器照着念。
4. 首次使用 → 在知识库录入产品口径 → 后续建议/话题自动引用。

---

## 9. 埋点与统计口径

| 事件 | 触发时机 | 字段 |
| --- | --- | --- |
| danmaku_recv | 收到弹幕 | 来源(mock/douyin/手动) |
| batch_flush | 一批触发处理 | 触发方式(条数/时间/手动)、条数 |
| suggestion_out | 产出建议 | source(template/local/cloud/fallback)、cost、耗时 |
| suggestion_adopt | 点击采纳 | source、从产出到采纳的秒数 |
| topic_generate | 点击生成话题 | 是否成功、来源(local/cloud) |
| kb_add/delete/import | 知识库变更 | 条数、文件类型 |
| budget_hit | 云端预算触顶 | 触发维度(次数/金额) |

> Demo 阶段以驾驶舱统计卡与运行日志作为轻量埋点呈现；正式版接入事件上报平台。

---

## 10. 非功能需求

| 类别 | 要求 |
| --- | --- |
| **性能** | 弹幕聚合与模板匹配毫秒级；AI 请求放后台线程，UI 不阻塞；单场日志仅保留 300 行防内存膨胀 |
| **可用性/降级** | 任一 AI 不可用自动降级；无 Key/无 Ollama 完整可跑；抖音断线自动重连 |
| **成本** | 单场云端 ≤ 50 次 / ≤ 2 元；prompt 前缀固定吃缓存；批量合并请求 |
| **数据与隐私** | 弹幕/知识库默认存本地；Key 优先环境变量，避免明文；抖音采集遵守平台规则（仅学习交流） |
| **兼容性** | Windows + Python 3.10；核心零第三方依赖；UI 深色主题按屏幕自适应 |
| **工程质量** | 27 个纯标准库回归用例；`py -3.10 -m compileall -q .` 全量语法校验；代码中文注释 |
| **合规声明** | 抖音接入移植自开源项目（AGPL-3.0），仅限学习交流，见 readme 免责声明 |

---

## 11. 风险与应对

| 风险 | 等级 | 影响 | 应对 |
| --- | --- | --- | --- |
| 抖音协议/签名频繁变更导致真实弹幕失效 | 高 | 真实源不可用 | 默认 mock 源保证演示；接入点可插拔；对照上游仓库更新 |
| AI 生成内容不恰当 | 中 | 主播误用引发舆情 | 主播始终保留采纳决策权；敏感词前置过滤；建议可一键忽略 |
| 云端费用超支 | 中 | 用户成本顾虑 | 预算硬闸门 + 实时花费展示 + 模板/本地优先 |
| 首次本地模型加载慢 | 低 | 首条回复延迟 | 本地失败 60s 缓存 + 自动切云端兜底 |
| 复杂问题模板覆盖不足 | 低 | 回落 AI 增加成本 | 模板可持续扩充；AI 批量处理摊薄单条成本 |

---

## 12. 版本计划

| 版本 | 范围 | 状态 |
| --- | --- | --- |
| **V1.0（当前）** | FR-01 ~ FR-15：完整闭环 Demo，含测试与文档 | 可运行可演示 |
| **V1.1** | FR-16 回归测试入库；模板知识库化管理（可配置模板 JSON）；建议采纳统计报表 | 规划 |
| **V2.0** | FR-17 多建议速选连发、FR-18 多直播间切换、FR-19 话术复盘导出；接入真实埋点上报 | 规划 |

---

## 13. 验收标准（Release Checklist）

- [ ] `py -3.10 main.py` 在**无 Key、无 Ollama** 环境正常启动，弹幕持续刷出，高频弹幕命中模板秒回；
- [ ] 驾驶舱统计卡/状态灯/日志/状态栏实时更新，无跨线程异常；
- [ ] 手动发送 + ⚡立即处理不卡 UI；采纳上屏形成闭环；
- [ ] 暂停/继续、聚合窗口在线调节即时生效；
- [ ] 提词器滚动、字号、话题生成可用；无 AI 时有明确提示；
- [ ] 知识库增删/检索/导入可用，重启后数据仍在；
- [ ] `py -3.10 -m unittest discover -s tests -t . -v` 全绿；
- [ ] `py -3.10 -m compileall -q .` 无语法错误。

---

## 14. 附录

| 术语 | 说明 |
| --- | --- |
| 聚合窗口 | 攒满 N 条或满 M 秒即触发一次批量处理，控制 AI 调用频率 |
| L1/L2/L3 | 模板 / 本地 Ollama / 云端 DeepSeek 三级回复引擎 |
| BudgetGuard | 云端预算器：50 次 / 2 元硬上限，超限自动降级 |
| 采纳上屏 | 把建议文案复制并以「主播」身份显示到弹幕流，形成回复闭环 |
| SimpleKB / ChromaKB | 关键词检索 / 语义向量检索两种知识库后端 |
| source 字段 | 建议来源：template / local / cloud / fallback，UI 依此着色 |
| 兜底(fallback) | 三级都无结果时输出的占位建议，保证界面不空 |

**相关文档**：`readme.md`（使用说明）、`方案.md`（技术方案与接口设计）、`tests/`（回归测试）、`main.py` / `config.json`（装配与配置）。
